{
  "buildCommand": "cd frontend && bun install && bun run build",
  "outputDirectory": "frontend/build/client",
  "installCommand": "npm install -g bun && bun --version",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
